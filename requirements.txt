# Web
fastapi==0.115.6
uvicorn[standard]==0.32.1
sqladmin==0.20.1
itsdangerous==2.2.0       # SessionMiddleware (sqladmin auth)

# Database
sqlalchemy==2.0.36
aiosqlite==0.20.0          # SQLite async driver (dev/small deployments)
# asyncpg==0.30.0          # PostgreSQL async driver (раскомментировать для prod)

# Bot
aiogram==3.17.0
aiogram[redis]>=3.17.0     # Redis FSM storage

# Utils
python-dotenv==1.0.1
python-multipart==0.0.12
wtforms>=3.1
Pillow==10.4.0
pillow-heif==0.21.0

# Auth
# ВАЖЛИВО: passlib 1.7.4 несумісний з bcrypt>=4.1 (AttributeError: module '__about__')
passlib==1.7.4
bcrypt==4.0.1

# MIME-detection (опціонально, потребує: apt install libmagic1)
# python-magic==0.4.27

# Redis для персистентного FSM storage
# Версія обмежена самим aiogram[redis] (потребує redis<5.1.0,>=5.0.1)
redis==5.0.8

# Міграції БД
alembic==1.13.3

# ── Тестові залежності ────────────────────────────────────────────────────────
# Встановлювати окремо або через: pip install -r requirements-dev.txt
# pytest==8.3.4
# pytest-asyncio==0.24.0
# pytest-timeout==2.3.1
# pytest-cov==6.0.0
